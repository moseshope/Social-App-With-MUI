import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#008000",
            light: "#00FF00"
        },
        secondary: {
            main: "#800080",
        },
        error: {
            main: "#FF0000"
        },
        otherColor: {
            main: "#A52A2A"
        }
    }
})