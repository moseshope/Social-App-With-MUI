import React from "react";
import { Box } from "@mui/material";

const Account = () => {
  return (
    <React.Fragment>
      <Box>
        Account
      </Box>
    </React.Fragment>
  );
};

export default Account;